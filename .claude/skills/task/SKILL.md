---
name: task
description: 設計書を基にタスクを分解し、作業順の Todo リスト（docs/TASK.md）を作成・更新する。
disable-model-invocation: true
argument-hint: "[対象セクションや機能 (optional)]"
---

# Task

設計書を分析し、実装順に並んだ Todo リストを作成する。

## Procedure

### 1. 設計書の確認

`docs/DESIGN.md` を読み込む:

- 内容がある場合 → 全セクションを分析し、実装すべき項目を洗い出す
- 空または不十分な場合 → ユーザーに警告する:

```text
⚠ docs/DESIGN.md が未作成または不十分です。
先に /design で設計書を作成することを推奨します。
このまま続行しますか？
```

`$ARGUMENTS` が指定されている場合は、該当する機能やセクションのみを対象にする。

### 2. 既存のタスクリストを確認

`docs/TASK.md` を読み込む:

- 内容がある場合 → 既存のタスクと進捗を把握し、追加・更新モードで進める。完了済みタスク (`[x]`) は変更しない
- 空の場合 → 新規作成モードで進める

### 3. タスクの分解

設計書の各セクションから実装作業を抽出し、以下のルールで分解する:

**階層化:**

- **大タスク** — 機能単位（例: ユーザー認証）
- **中タスク** — コンポーネント単位（例: ログイン API）
- **小タスク** — 実装単位（例: POST /auth/login のバリデーション実装）

小タスクは **1つの作業セッションで完了できる粒度** を目安にする。

**順序:**

上から順にやれば迷わない順序で並べる。具体的には:

1. 環境構築・初期設定
2. データモデル・DB スキーマ
3. バックエンド（API、ビジネスロジック）
4. フロントエンド（画面、コンポーネント）
5. 連携・結合（API 接続、E2E フロー）
6. テスト・品質
7. デプロイ・インフラ

同じ階層内では、依存される側を先に配置する。

### 4. タスクリストの作成

以下のフォーマットで `docs/TASK.md` を作成する:

```markdown
# タスクリスト

## 1. 大タスク名

### 1.1 中タスク名

- [ ] 小タスクの説明
- [ ] 小タスクの説明

### 1.2 中タスク名

- [ ] 小タスクの説明
- [ ] 小タスクの説明

## 2. 大タスク名

### 2.1 中タスク名

- [ ] 小タスクの説明
```

ルール:

- チェックボックス (`- [ ]`) は小タスクにのみ付ける
- 大タスク・中タスクは見出し（番号付き）で構造化する
- 各小タスクは何をすればいいか一目でわかる記述にする（「〜を実装」「〜を設定」「〜を作成」）
- 曖昧な表現（「必要に応じて対応」「適宜実装」）は使わない

### 5. レビューと確定

作成したタスクリストをユーザーに提示し、フィードバックを受ける:

- タスクの追加・削除・順序変更 → 反映する
- 粒度の調整（「もっと細かく」「まとめて」） → 該当タスクを再分解またはマージする
- 承認 → ファイルに書き込んで完了

### 6. 完了報告

```text
Task List Updated
═══════════════════════════════════════
File:     docs/TASK.md
Mode:     <新規作成 / 更新>
Tasks:    <総タスク数>

Next steps:
  - /implement でタスクを上から順に実装する
═══════════════════════════════════════
```
