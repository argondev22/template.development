# CLAUDE.md

## メモリ管理

プロジェクト固有の情報を `.claude/MEMORY.md` に保存する:

- アーキテクチャ決定、技術選定とその理由
- プロジェクト固有の制約・ルール
- 外部リソースの参照先（チケット、ダッシュボード等）

保存しないもの:

- 個人の好みやスタイル（→ ユーザーレベルの `~/.claude/MEMORY.md` に保存）
- コードや git 履歴から読み取れる情報
- 一時的なタスク状態
- 重複を避けるため、保存前に既存のメモリを確認する
